{
  "name": "track-it",
  "private": true,
  "scripts": {
    "build": "cd PlaidBackend && npm run build",
    "start": "cd PlaidBackend && npm run start",
    "dev": "cd PlaidBackend && npm run dev"
  },
  "engines": {
    "node": ">=20"
  }
}
